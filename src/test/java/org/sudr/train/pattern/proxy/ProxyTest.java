package org.sudr.train.pattern.proxy;

/**
 * Allows for object level access control by acting as a pass through entity or
 * a placeholder object.
 * 
 * Looks similar to the Adapter. But unlike the adapter which may expose a
 * different interface to the client that the adaptee, the proxy will expose the
 * same interface as to the client as the object being proxied.
 * 
 */
public class ProxyTest {

}
